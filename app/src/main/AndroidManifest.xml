<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="mwo.lab.tapswap">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".LaunchScreenActivity" android:theme="@style/LaunchScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".DashboardActivity"></activity>

        <activity android:name=".MyItemsActivity"></activity>
        <activity android:name=".DiscoverActivity"></activity>
        <activity android:name=".AddItemActivity"></activity>
        <activity android:name=".MyAccountActivity"></activity>
        <activity android:name=".WhateverActivity"></activity>
        <activity android:name=".MySwapsActivity"></activity>

    </application>

</manifest>
